# 本番環境用設定例
# 注意: 実際の値は絶対にリポジトリにコミットしないこと

# データベース設定（強固なパスワードに変更）
DB_HOST=localhost
DB_USER=valerosso_prod_user
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
DB_NAME=valerosso_production
DB_PORT=3306

# MySQL設定（強固なパスワードに変更）
MYSQL_ROOT_PASSWORD=CHANGE_THIS_TO_VERY_STRONG_PASSWORD

# セッション設定（ランダムな長い文字列に変更）
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_LONG_STRING_FOR_PRODUCTION

# 環境設定
NODE_ENV=production
PORT=3000

# SSL設定（本番環境で使用）
SSL_CERT_PATH=/path/to/ssl/cert.pem
SSL_KEY_PATH=/path/to/ssl/private.key

# 本番環境セットアップ手順:
# 1. このファイルを .env.production にコピー
# 2. 全ての CHANGE_THIS_TO_* を実際の値に変更
# 3. 初期管理者パスワードを変更
# 4. HTTPS証明書を設定